import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#c64756",
      contrastText: "#ffffff"
    },
    secondary: {
      main: "#495057",
      contrastText: "#ffffff"
    }
  }
});